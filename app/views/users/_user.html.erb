<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "smazat", user, method: :delete, data: { confirm: "Potvrdit smazání uživatele #{user.name}" } %>
  <% end %>
</li>