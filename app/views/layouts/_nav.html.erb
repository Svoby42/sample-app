<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"><h2 class="text-white">Aplikace</h2></a>
    <button class="navbar-toggler col-md-2 bg-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      Menu
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="nav navbar-nav ml-auto">
        <li class="nav-item"><%= link_to "Domů", home_path, {:class => "nav-link"} %></li>
        <li class="nav-item"><%= link_to "Help", help_path, {:class => "nav-link"}%></li>
      </ul>
    <% if logged_in? %>
      <ul class="nav navbar-nav ms-auto">
        <li><%= link_to "Uživatelé", '#', {:class => "nav-link"} %></li>
      </ul>
      <div>
        <button class="navbar-toggler col-md-2 bg-secondary" type="button" data-bs-toggle="collapse" data-bs-target="#userContent"
                aria-controls="userContent" aria-expanded="false" aria-label="Toggle navigation">
          Uživatel
        </button>
        <div class="collapse navbar-collapse" id="userContent">
          <ul class="nav navbar-nav">
            <li class="nav-item"><%= link_to "Profil", current_user, {:class => "nav-link"} %></li>
            <li class="nav-item"><%= link_to "Nastavení", edit_user_path(current_user), {:class => "nav-link"} %></li>
            <li class="dropdown-divider"></li>
            <li class="nav-item"><%= link_to "Odhlásit", logout_path, method: :get, :class => "nav-link" %></li>
          </ul>
        </div>
      </div>
    <% else %>
      <ul class="nav navbar-nav ms-auto">
        <li class="nav-item"><%= link_to "Log In", login_path, {:class => "nav-link"} %></li>
      </ul>
    <% end %>
    </div>
  </div>
</nav>